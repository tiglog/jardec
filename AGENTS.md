# AGENTS

## 行为准则（最高优先级，违反即错误）

- **非明确要求，不得修改任何内容。** 只有在用户明确指示"改"、"修改"、"实现"、"创建"、"提交"等操作动词时，才能动手改动文件。如果用户只是在提问、讨论或分析，只输出分析结论，不执行任何修改操作。
- **不得随意访问外部目录。** 除非用户明确提供路径，或为完成已授权任务所必需且已先说明用途，否则不得读取、遍历或搜索项目工作区之外的目录。
- **任何非平凡代码变更必须先走 OpenSpec 流程**，见下方"变更流程"章节。

## 项目范围

这是一个 **Go 命令行工具**，对 JAR 执行 **jadx 优先、Procyon 回退** 的反编译流程。CLI 框架为 **`urfave/cli`**。

## 当前架构

- CLI 入口: `cmd/jardec/`
- 内部模块: `internal/cli/`、`internal/decompiler/`、`internal/jar/`、`internal/merge/`、`internal/report/`
- 测试数据: `testdata/`

## 变更流程（强制执行）

所有非平凡的代码变更 **必须** 走 OpenSpec 流程。在写任何代码之前，先通过以下判断门：

Makefile target 的新增、删除或调整不视为代码变更，无需走 OpenSpec 流程。

### 判断门：这个变更是否"平凡"？

一个变更只有在 **同时满足以下所有条件** 时才是"平凡的"（可以绕过 OpenSpec）：
- 仅修复注释或字符串字面量中的拼写错误
- 仅调整格式/空白符
- **不**修改任何函数签名、结构体字段、配置项 key 或逻辑路径
- **不**新增或删除任何代码行为

**以下都属于非平凡变更，必须走流程：**
- 新增功能、新增 flag、新增配置项
- 修改结构体字段、函数签名或接口
- 校验逻辑、管线逻辑、命令处理逻辑的变更
- 合并或删除配置/模式字段
- 新增或变更外部依赖

### 流程步骤
1. `openspec-propose` → 生成 proposal.md + design.md + specs/*.md + tasks.md
2. `openspec-apply-change` → 逐项实现任务并标记 `[x]`
3. `openspec-archive-change` → 归档完成的变更

如果你冒出"这个改动很小，跳过吧"的念头——它不是平凡的，提 proposal。

## 实现规则

- 遵循 **TDD**：先写或更新失败的测试/夹具，然后实现最小改动使其通过，最后重构。
- 以 **top-level class** 作为首版的完整性基线。
- 对整个 JAR 运行一次 **jadx**，然后从生成输出和诊断信息中分类出需要重试的候选项。
- **严格**分类输出。显式的重试信号包括：
  - 缺少 `.java` 输出
  - 空或近似空的输出
  - 工具报告的逐类失败
  - 已知的失败占位符文本
  - 生成源码中的 `JADX WARN` 标记
- 对失败类用 **Procyon** 逐类重试。
- 允许成功的 Procyon 重试结果 **替换** 最终合并目录中被标记的 jadx 输出。
- 保持逐类报告可审计：最终状态、来源（`jadx` 或 `procyon`）以及重试/失败原因。

## v1 的非目标

- 不保证匿名类、合成类、lambda 生成类或其他非顶层类的完整性。
- 不将 jadx 或 Procyon 二进制文件纳入仓库。
- 不在显式失败证据之外添加语义质量评分。

## 工程偏好

- 使用 **`urfave/cli`** 进行命令定义、flag 解析和帮助输出。
- 优先使用 Go 标准库，除非某个依赖明确降低风险。
- 文件/路径处理保持确定性且跨平台安全。
- 对逐类 Procyon 重试使用隔离的临时工作区；不要让并发 worker 直接写入最终输出树。
- 倾向显式错误传播而非静默降级。
- 当输出被替换时，在报告中保留来源信息。

## 扩展实现之前

- 在构建主要管线行为之前，先建立测试夹具和命令级测试。
- 尽早验证选定的 Procyon 单类调用模式。
- 添加覆盖以下场景的夹具：干净的 jadx 输出、带 `JADX WARN` 的回退、成功的 Procyon 恢复、歧义的重试输出以及不可恢复的失败。
